{{#if resume.references}}
{{#>section title=(sectionTitle resume "references" "References")}}
	{{#each resume.references}}
	<blockquote class="item">
		{{this.reference}}
		<br />
		─ {{this.name}}
	</blockquote>
	{{/each}}
{{/section}}
{{/if}}